# Server Configuration
PORT=3000
NODE_ENV=production
JWT_SECRET=attendance_jwt_secret_key_2026

# MySQL Database Configuration (Optional - in-memory mock with full seed data active by default)
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=attendance_management

# Gemini API Key (Required for AI Studio platform capabilities)
GEMINI_API_KEY=
